# Learnings - agent-news-v01

## 2026-05-10 Session start
- Active plan: `.sisyphus/plans/agent-news-v01.md`.
- Work in current project directory; no worktree was specified.
- Must use `conda run -n vnpy43` for all Python/test/backfill commands.
- User selected independent SQLite storage at `~/.vntrader/agent_news.db`.
- Public/no-login/no-paid source policy; record coverage gaps instead of hiding them.
- Offline historical backfill only for v0.1; no scheduler/realtime daemon.
- Do not commit unless user explicitly asks.

## 2026-05-10 Task 1 contracts
- Added `myQuant.news_ingestion` package skeleton with dataclass contracts and v0.1 enums exported from package root.
- Contract validation follows vn.py `vt_symbol` convention via `vnpy.trader.utility`: `300750.SZSE` parses to `symbol=300750`, `exchange=SZSE`, and regenerates the same string.
- Numeric validation rejects `impact_strength` and `confidence` outside `[0.0, 1.0]`; QA commands passed for valid signal, invalid confidence, full contract suite, and package import.
- Removed generated bytecode artifacts from `myQuant/news_ingestion/` and `myQuant/news_ingestion/tests/`; use `PYTHONDONTWRITEBYTECODE=1` for future verification if bytecode artifacts must be avoided.

## 2026-05-10 Task 2 SQLite storage
- Added peewee-backed `AgentNewsSqliteRepository` under `myQuant.news_ingestion.storage`, defaulting to `~/.vntrader/agent_news.db` via `vnpy.trader.utility.get_file_path` and supporting explicit temp DB paths for tests/CLI.
- Schema initialization creates the nine required `agent_*` tables with required unique constraints and indexes; raw-news upsert handles both `(source, source_item_id)` and `(source, content_hash)` idempotency, including missing source item IDs.
- Verification passed: `PYTHONDONTWRITEBYTECODE=1 conda run -n vnpy43 python -m pytest myQuant/news_ingestion/tests/test_storage_sqlite.py -q` (8 passed) and `PYTHONDONTWRITEBYTECODE=1 conda run -n vnpy43 python -m pytest myQuant/news_ingestion/tests -q` (12 passed).
- Verified no `__pycache__` or `.pyc` artifacts remain under `myQuant/news_ingestion`.

## 2026-05-10 Task 2 final verification update
- After adding schema constraint/index coverage and repository isolation checks, final verification passed: `PYTHONDONTWRITEBYTECODE=1 conda run -n vnpy43 python -m pytest myQuant/news_ingestion/tests/test_storage_sqlite.py -q` (9 passed) and `PYTHONDONTWRITEBYTECODE=1 conda run -n vnpy43 python -m pytest myQuant/news_ingestion/tests -q` (13 passed).

## 2026-05-10 Task 3 stock profiles/universe
- Added `myQuant.news_ingestion.profiles` with read-only market DB discovery using `SELECT symbol, exchange FROM dbbaroverview WHERE interval='d' ORDER BY symbol;`; results normalize through existing `generate_vt_symbol` helpers.
- Seeded exactly the 10 requested `StockProfile` entries and added `persist_discovered_stock_profiles()` to save discovered matching profiles into `AgentNewsSqliteRepository.agent_stock_profile`.
- Verification passed: discovery QA (1 passed), missing-profile QA (1 passed), `test_stock_profiles.py` (4 passed), and full `myQuant/news_ingestion/tests` suite (17 passed) with `PYTHONDONTWRITEBYTECODE=1 conda run -n vnpy43`.
- Verified no `__pycache__` or `.pyc` artifacts remain under `myQuant/news_ingestion` after Task 3 tests.

## 2026-05-10 Task 4 source adapter interface
- Added `myQuant.news_ingestion.sources` with `SourceFetchResult`, abstract `BaseNewsSource.fetch(query: NewsQuery)`, injectable `PoliteHttpClient`, `HttpResponse`, `HttpRequestResult`, and live-test helpers gated by `AGENT_NEWS_LIVE_TEST=1`.
- HTTP wrapper defaults: timeout `15s`, retry count `2` (three total attempts), exponential backoff for `429/5xx`, request interval `1.0s`, and `User-Agent: Mozilla/5.0 AgentNewsV01Research`; normal tests inject fake transports and make no live network calls.
- Targeted verification passed: `PYTHONDONTWRITEBYTECODE=1 conda run -n vnpy43 python -m pytest myQuant/news_ingestion/tests/test_source_base.py -q` (4 passed), including structured `429` retry failure and user-agent coverage.
- Broader verification passed: `PYTHONDONTWRITEBYTECODE=1 conda run -n vnpy43 python -m pytest myQuant/news_ingestion/tests -q` (21 passed).
- LSP diagnostics are clean for `myQuant/news_ingestion/sources/base.py`, `myQuant/news_ingestion/sources/__init__.py`, and `myQuant/news_ingestion/tests/test_source_base.py`; verified no `__pycache__` or `.pyc` artifacts under `myQuant/news_ingestion`.
